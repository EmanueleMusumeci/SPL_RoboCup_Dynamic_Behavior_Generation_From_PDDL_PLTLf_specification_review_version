walkSpeed = 0.8;

initialWaitTime = 50;
ballNotSeenTimeout = 3500;
changeTargetTimeout = 4000;
realignmentTimeout = 5000;

distanceFromGroundLineCardChangeThreshold = 2000;

ballAlignThreshold_degrees = 5;

ballPositionChangedThreshold = 800;
targetPositionChangedThreshold = 500;

smallBallAlignmentRange = {
    min = -8;
    max = 8;
};

ballAlignmentRange = {
    min = -20;
    max = 20;
};

approachXRange = {
    min = 100; 
    max = 700;
};

approachYRange = {
    min = -100; 
    max = 100;
};

ballOffsetY = -75;

maxKickWaitTime = 5000;

nearbyObstaclesRadius = 1500;

DEBUG_MODE = false;
